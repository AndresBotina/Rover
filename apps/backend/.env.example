# =============================================================================
# Plantilla de configuración del backend de Rover (HU-0.8).
#
# Uso en LOCAL:   cp .env.example .env   y ajusta lo que necesites.
#   El .env real está git-ignorado y NUNCA se commitea.
# En PRODUCCIÓN (Render) NO hay archivo .env: las variables se configuran en
#   el panel del servicio (Environment). ROVER_ENV ya lo fija render.yaml.
#
# Todas las variables llevan el prefijo ROVER_ (ver app/core/config.py).
# Los valores de este archivo son EJEMPLOS/placeholders: jamás pongas aquí un
# valor real de un secreto.
# =============================================================================

# --- App (no sensible; con defaults sensatos, puedes omitirlas) --------------

# Nombre de marca visible al usuario.
ROVER_APP_NAME=Rover

# Ambiente: local | test | production. Cualquier otro valor impide arrancar.
ROVER_ENV=local

# Versión reportada por /v1/health. Normalmente NO se define: la fuente de
# verdad es app.__version__ (este override existe solo para casos puntuales).
# ROVER_VERSION=0.1.0

# Documentación interactiva (/docs, /redoc, /openapi.json). Si NO se define,
# decide el ambiente: activa fuera de producción, APAGADA en producción (el
# esquema es el mapa completo de la API y no hace falta publicarlo). Definirla
# fuerza el valor — p. ej. true para depurar en producción (HU-1.9).
# ROVER_ENABLE_DOCS=true

# --- Logging (HU-1.12; no sensible) ------------------------------------------
# Formato de los logs: json | text. Si NO se define, decide el ambiente:
#   - production → json  (lo consume una herramienta de monitoreo, que filtra
#     por campo: level, request_id, status, duration_ms…)
#   - local/test → text  (lo lee una persona en una terminal)
# Definirla fuerza el valor: p. ej. json en local para ver exactamente lo que
# se va a producción.
# ROVER_LOG_FORMAT=json

# --- CORS (HU-1.11; no sensible) ---------------------------------------------
# Orígenes desde los que un NAVEGADOR puede llamar a esta API, separados por
# comas y SIN barra final (el origen es esquema + host + puerto). El móvil
# (Expo) no pasa por CORS: esto solo afecta a la web.
#
# Si NO se define, decide el ambiente:
#   - local/test  → http://localhost:3000 y http://127.0.0.1:3000 (la web de
#     la Épica 3 funciona sin configurar nada). Para un navegador son orígenes
#     DISTINTOS, por eso van los dos.
#   - production  → NINGUNO. No se cae a "*": un despliegue al que se le
#     olvidó la variable queda cerrado a los navegadores, no abierto a todos.
#     La API sigue arrancando y sirviendo (móvil, curl, servicios), y avisa
#     con un warning en el log de arranque.
#
# En PRODUCCIÓN "*" está PROHIBIDO: la app NO ARRANCA si se configura (hay que
# enumerar los orígenes exactos). Fuera de producción sí se admite, como
# escape hatch para depurar.
#
# En Render se configura en el panel del servicio → Environment.
# Ejemplo local (solo si necesitas un puerto u origen distinto del default):
# ROVER_CORS_ORIGINS=http://localhost:3000,http://localhost:8081
# Ejemplo producción (obligatorio en cuanto exista la web):
# ROVER_CORS_ORIGINS=https://rover.app,https://www.rover.app

# --- Rate limiting (HU-1.7; no sensible, con defaults sensatos) --------------
# Límites en "N peticiones por ventana". El algoritmo y el porqué de cada valor
# están en app/core/rate_limit.py y app/core/config.py.

# Interruptor general. Se apaga para desactivar los límites en un incidente sin
# tener que desplegar.
# ROVER_RATE_LIMIT_ENABLED=true

# GLOBAL, por IP: protección base de toda la API (120/min ≈ 2 req/s).
# ROVER_RATE_LIMIT_DEFAULT_LIMIT=120
# ROVER_RATE_LIMIT_DEFAULT_WINDOW_SECONDS=60

# AUTH, por IP: /v1/auth/login y /v1/auth/register. Mucho más estricto — es
# donde se adivinan contraseñas y se crean cuentas en masa.
# ROVER_RATE_LIMIT_AUTH_LIMIT=10
# ROVER_RATE_LIMIT_AUTH_WINDOW_SECONDS=60

# USUARIO, por id del token: la cuota de la cuenta autenticada.
# ROVER_RATE_LIMIT_USER_LIMIT=60
# ROVER_RATE_LIMIT_USER_WINDOW_SECONDS=60

# Multiplicador de la cuota de usuario para los planes de pago. 1.0 = hoy free
# y pro valen lo mismo; es el enganche de la Épica 5 (monetización).
# ROVER_RATE_LIMIT_PRO_MULTIPLIER=1.0

# Saltos de proxy DE CONFIANZA delante de la app, para leer la IP real de
# X-Forwarded-For sin que el cliente pueda falsificarla. Render pone
# exactamente 1 (su edge). Con un CDN delante serían 2. Con 0 la cabecera se
# IGNORA y se usa el peer TCP (exposición directa, p. ej. en local).
# ROVER_RATE_LIMIT_TRUSTED_PROXIES=1

# --- Base de datos / Supabase (Épica 1) — SECRETOS ---------------------------
# Valores reales SOLO en tu .env local o en Render → Environment. En producción
# son OBLIGATORIAS (la app no arranca si faltan: ver _REQUIRED_IN_PRODUCTION
# en config.py).

# Conexión a Supabase Postgres (SECRETO: incluye la contraseña).
# RECOMENDADO (local y producción): la URL del TRANSACTION POOLER (Supavisor,
# puerto 6543) — la conexión directa resuelve a IPv6 y ni local ni Render
# tienen salida IPv6. El código detecta el pooler por la URL y desactiva los
# prepared statements de asyncpg automáticamente (ver app/core/database.py).
# Va TAL CUAL la entrega Supabase (prefijo postgresql://): el driver async
# (postgresql+asyncpg://) lo añade el código — NO la guardes transformada.
# OBLIGATORIA en producción (HU-1.1).
ROVER_DATABASE_URL=postgresql://postgres.TU-PROYECTO:TU-PASSWORD@aws-1-TU-REGION.pooler.supabase.com:6543/postgres

# URL del proyecto de Supabase. No es un secreto en sí (aparece en cualquier
# request al API desde un cliente), pero SÍ es OBLIGATORIA en producción: sin
# ella no hay a quién llamar (HU-1.3, registro/login vía Supabase Auth).
ROVER_SUPABASE_URL=https://tu-proyecto.supabase.co

# Anon/public key de Supabase (SECRETO por convención del proyecto — SecretStr
# para no filtrarla en logs/repr; Supabase la considera segura para clientes).
# Es la que usa el REGISTRO (HU-1.3) y el LOGIN (HU-1.4): permisos de cliente
# público, sin privilegios elevados. OBLIGATORIA en producción.
ROVER_SUPABASE_ANON_KEY=coloca-aqui-tu-anon-key

# Service-role key de Supabase (SECRETO — SOLO BACKEND, JAMÁS en clientes web
# ni móvil, JAMÁS en logs). Permisos ADMINISTRATIVOS totales: se salta Row
# Level Security. Queda configurada para operaciones administrativas futuras;
# el registro/login (HU-1.3/1.4) NO la usa. OBLIGATORIA en producción.
ROVER_SUPABASE_SERVICE_ROLE_KEY=coloca-aqui-tu-service-role-key

# --- Proveedor de LLM (Épica 2, HU-2.1) --------------------------------------
# El backend habla con la INTERFAZ de app/services/llm, nunca con un proveedor
# concreto. Estas variables deciden cuál hay detrás: cambiar de proveedor
# OpenAI-compatible (o de modelo) es cambiar entorno, NO tocar código.

# API key del proveedor (SECRETO — hoy, la de DeepSeek). Valor real SOLO en tu
# .env local o en Render → Environment; nunca en el repo ni en los logs
# (SecretStr la enmascara en repr/logs). OBLIGATORIA en producción: sin ella no
# hay agente y la app no arranca.
ROVER_LLM_API_KEY=coloca-aqui-tu-api-key

# URL base del proveedor (NO es secreta). Default: DeepSeek. Se respeta el path
# que traiga —la ruta /chat/completions se le concatena—, así que sirve
# cualquier endpoint OpenAI-compatible, con o sin sufijo de versión:
#   https://api.deepseek.com           (default)
#   https://openrouter.ai/api/v1
# ROVER_LLM_BASE_URL=https://api.deepseek.com

# Modelo a usar (NO es secreto). Single-model por decisión de arquitectura de
# la Épica 2; el modelo de VISIÓN futuro no entraría por aquí, sino como otro
# proveedor en el registro (app/services/llm/registry.py).
# ROVER_LLM_MODEL=deepseek-v4-flash

# --- Parámetros de generación (no secretos, con defaults sensatos) -----------
# Variedad de las respuestas (0.0–2.0). 0.7 = conversación natural; 0.0 = casi
# determinista. El tool-calling (HU-2.6) va con este mismo valor: en la práctica
# el modelo decide bien qué herramienta usar, y bajarlo volvería las respuestas
# planas para ganar una precisión que no hacía falta.
# ROVER_LLM_TEMPERATURE=0.7

# Techo de tokens de SALIDA por respuesta: límite de costo y protección contra
# una generación que se dispare.
# ROVER_LLM_MAX_OUTPUT_TOKENS=2048

# Timeout de una llamada al LLM, en segundos. Generoso frente al de Supabase
# Auth (10 s): generar una respuesta larga tarda, y cortar a mitad gasta los
# tokens igual sin entregar nada.
# ROVER_LLM_TIMEOUT_SECONDS=60

# Modo de razonamiento del modelo: disabled (default) | enabled |
# provider_default. DeepSeek V4 razona por defecto y ese razonamiento SE COBRA
# como salida (~1.163 tokens y ~15 s para una respuesta de cuatro frases), así
# que el chat va en non-think. Se reactiva aquí, sin tocar código, el día que
# haya tareas que lo valgan (itinerarios largos). `provider_default` omite el
# campo del cuerpo: es la salida para un proveedor OpenAI-compatible que no lo
# conozca y rechace parámetros desconocidos.
# ROVER_LLM_THINKING=disabled

# --- Memoria del chat: ventana de contexto (HU-2.5) --------------------------
# Rover recuerda porque en cada turno se le REENVÍA la conversación al modelo.
# Eso se paga en cada turno, así que el historial se recorta a un presupuesto.

# Tokens de HISTORIAL que se reenvían. No incluye el system prompt (constante y
# casi siempre servido de caché) ni el mensaje nuevo: esos van encima. 4.000 son
# unos 20-30 turnos normales. Recortar no es solo ahorrar: con entradas largas
# el modelo atiende peor a lo que queda por el medio.
# ROVER_CHAT_CONTEXT_TOKEN_BUDGET=4000

# Caracteres por token que asume el estimador. 3,0 es PESIMISTA a propósito (el
# español real ronda 3,5-4,0): sobreestimar recorta memoria vieja de más,
# subestimar manda más contexto del que cabe y el proveedor responde 400. Se
# calibra contra el `llm_input_tokens` real que ya registra la instrumentación.
# ROVER_CHAT_CONTEXT_CHARS_PER_TOKEN=3.0

# --- Herramientas del agente (HU-2.6) ----------------------------------------
# Cuántas llamadas al modelo puede gastar UN turno de chat. El camino normal usa
# 2 (el modelo pide una herramienta → redacta con el resultado); la tercera deja
# sitio a encadenar dos consultas legítimas sin abrir la puerta a un bucle.
# Acota COSTO más que tiempo: cada vuelta es una llamada facturada con un
# contexto que además crece. Al llegar al límite, la última llamada se hace SIN
# ofrecer herramientas, así que el turno siempre acaba en una respuesta escrita.
# ROVER_TOOL_LOOP_MAX_ITERATIONS=3

# --- Proveedor de clima (HU-2.6) ---------------------------------------------
# La herramienta del clima habla con la INTERFAZ de app/services/weather, nunca
# con un proveedor concreto. Hoy detrás hay OpenWeatherMap (geocoding + clima
# con la misma key y plan gratuito suficiente).
#
# CAMBIO A OPEN-METEO (la alternativa SIN key): escribir
# app/services/weather/openmeteo.py cumpliendo el Protocol de base.py, cambiar
# _construir_proveedor() en app/services/weather/registry.py y borrar la
# variable de abajo. No se toca el loop, ni el endpoint, ni la definición de la
# herramienta que ve el modelo. (El uso comercial de cualquiera de los dos se
# revisa en la Épica 5.)

# API key de OpenWeatherMap (SECRETO). Se saca gratis en
# https://home.openweathermap.org/api_keys.
#
# NO es obligatoria en producción, a diferencia de ROVER_LLM_API_KEY: sin ella
# la herramienta del clima simplemente NO SE REGISTRA (el modelo no puede pedir
# lo que no se le ofrece) y Rover conversa igual, solo que sin datos del tiempo.
# Queda un warning en el log de arranque del catálogo de herramientas.
#
# OJO: una key RECIÉN CREADA tarda un rato (hasta un par de horas) en
# activarse, y mientras tanto la API responde 401. Eso se degrada como
# cualquier otro fallo de herramienta —la conversación no se cae— y deja en el
# log una línea que lo dice explícitamente. Si Rover contesta que no pudo
# consultar el clima justo después de crear la key, es esto: espera.
ROVER_WEATHER_API_KEY=coloca-aqui-tu-api-key-de-openweathermap

# URL base del proveedor de clima (NO es secreta).
# ROVER_WEATHER_BASE_URL=https://api.openweathermap.org

# Idioma de la descripción del tiempo ("nubes dispersas" vs. "scattered
# clouds") y del nombre local del lugar ("Bogotá", no "Bogota"). El modelo
# repite lo que le damos, así que esto se nota en la respuesta.
# ROVER_WEATHER_LANGUAGE=es

# Timeout de una consulta de clima, en segundos. Mucho más corto que el del LLM:
# ocurre DENTRO de un turno, con el usuario esperando y con una llamada al
# modelo todavía por delante. Si no responde en 10 s, lo útil es degradar.
# ROVER_WEATHER_TIMEOUT_SECONDS=10
