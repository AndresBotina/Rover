{
  "name": "@rover/shared",
  "version": "0.0.0",
  "private": true,
  "//": "Tipos compartidos + cliente API tipado (HU-0.7). Se consume como fuente TS (exports → src); en Épica 1 los tipos pasarán a generarse desde el OpenAPI del backend.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "test": "node --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
